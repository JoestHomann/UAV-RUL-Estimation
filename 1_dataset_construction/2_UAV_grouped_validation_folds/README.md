# Step 2: UAV-grouped validation folds

**Reads:** Step 1 `train_flight_cycles.csv`

**Writes:** `artifacts/outer_folds.csv`, `artifacts/inner_folds.csv`, `artifacts/fold_config.json`

This step adds reproducible group assignments without changing the Step 1 summary.
