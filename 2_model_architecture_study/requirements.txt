# Resolves the CUDA-enabled torch build pinned below.
--extra-index-url https://download.pytorch.org/whl/cu126

# Builds sequence tensors, padding masks, side features, and robust scalers.
numpy>=1.26,<3

# Reads and structurally verifies the Phase 1 CSV and compressed CSV artifacts.
pandas>=3.0,<4

# Provides strict, nested validation for the architecture study settings.
pydantic>=2.13,<3

# Implements Ridge, Elastic Net, Random Forest, Extra Trees, RBF-SVR, and scaling.
scikit-learn>=1.7,<2

# Persists complete fitted adapters with their preprocessing and model state.
joblib>=1.5,<2

# Runs deterministic automatic tuning within each architecture family.
optuna>=4.8,<5

# Implements the regularized gradient-boosted-tree architecture.
xgboost>=3.0,<4

# Implements deterministic CPU CatBoost regression.
catboost>=1.2.10,<2

# Implements the MLP, TCN, LSTM, and optional Transformer architectures.
# Pinned to the verified CUDA build, not a range, so training doesn't
# silently fall back to the CPU-only PyPI build.
torch==2.13.0+cu126

# Provides mandatory real-time monitoring for every Phase 2 training run.
tensorboard>=2.20,<3

# Creates the fixed Step 7 architecture-comparison figures.
matplotlib>=3.10,<4
