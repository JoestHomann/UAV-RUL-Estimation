# Step 8: Validation metrics

**Reads:** a prediction table when evaluating a model

**Writes:** `artifacts/metric_specification.json` by default; model-specific metric tables are written beside that model's predictions.

The module defines R², RMSE, MAE, bias, grouped reports, and UAV-bootstrap uncertainty.
